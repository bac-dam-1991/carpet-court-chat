import { Box } from '@mui/material';

export const LoadingDot = () => {
	return (
		<Box
			sx={{
				width: 12,
				height: 12,
				backgroundColor: 'white',
				borderRadius: '50%',
			}}
		/>
	);
};
